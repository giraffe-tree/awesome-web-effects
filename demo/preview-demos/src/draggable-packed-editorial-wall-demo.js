import { animate } from 'motion';import{installPreviewController,markPreviewFailure}from'../shared.js';import{cosineLoop,clamp}from'./batch-b-utils.js';
try{const wall=document.querySelector('#editorial-wall'),tiles=[...document.querySelectorAll('.tile')];const shifts=[[0,0],[0,17],[-22,0],[15,-8],[31,-6],[-18,-25]];const motions=tiles.map((tile,i)=>{const c=animate(tile,{x:[0,shifts[i][0]],y:[0,shifts[i][1]],rotate:[0,(i%2?1:-1)*2]},{duration:1,ease:[.22,1,.36,1]});c.pause();return c});let dragged=null,start=null,offset={x:0,y:0},forced=null;tiles.forEach(tile=>{tile.addEventListener('pointerdown',e=>{dragged=tile;start={x:e.clientX,y:e.clientY};tile.setPointerCapture(e.pointerId);tile.classList.add('dragging')});tile.addEventListener('pointermove',e=>{if(dragged!==tile)return;offset={x:e.clientX-start.x,y:e.clientY-start.y};tile.style.translate=`${offset.x}px ${offset.y}px`});tile.addEventListener('pointerup',()=>{tile.classList.remove('dragging');tile.style.translate='';dragged=null;forced=forced===1?0:1})});wall.addEventListener('keydown',e=>{if(!['Enter',' '].includes(e.key))return;e.preventDefault();forced=forced===1?0:1});window.__PREVIEW_RUNTIME_ASSERT__=()=>motions.length===6&&motions.every(c=>typeof c.pause==='function'&&c.duration===1);installPreviewController({id:'draggable-packed-editorial-wall',library:'motion@12.42.2',renderer:'dom',render:t=>{const p=forced??cosineLoop(t);motions.forEach(c=>{c.time=c.duration*clamp(p)})},ready:document.fonts.ready})}catch(error){markPreviewFailure(error)}
