import{animate}from'motion';import{installPreviewController,markPreviewFailure}from'../shared.js';import{loopTime,clamp}from'./batch-b-utils.js';
try{const stage=document.querySelector('#marquee-stage'),track=document.querySelector('#marquee-track'),windowElement=document.querySelector('#marquee-window'),meter=document.querySelector('#velocity-meter');const motion=animate(track,{x:[0,-420]},{duration:3,ease:'linear'});motion.pause();let velocity=1,lastInput=0;function inject(delta){velocity=clamp(velocity+delta,.35,2.8);lastInput=performance.now()}stage.addEventListener('wheel',e=>{e.preventDefault();inject(Math.abs(e.deltaY)*.004)},{passive:false});stage.addEventListener('keydown',e=>{if(!['ArrowLeft','ArrowRight'].includes(e.key))return;e.preventDefault();inject(e.key==='ArrowRight'?.35:-.2)});window.__PREVIEW_RUNTIME_ASSERT__=()=>motion.duration===3&&typeof motion.pause==='function'&&track.scrollWidth>400;installPreviewController({id:'velocity-reactive-marquee',library:'motion@12.42.2',renderer:'dom',render:t=>{if(performance.now()-lastInput>80)velocity+=(1-velocity)*.06;motion.time=loopTime(t*velocity);windowElement.style.setProperty('--skew',`${(velocity-1)*-5}deg`);meter.textContent=`${velocity.toFixed(2)}×`},ready:document.fonts.ready})}catch(error){markPreviewFailure(error)}
