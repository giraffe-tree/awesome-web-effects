import './batch-a-qa.js';
import{animate}from'motion';import{installPreviewController,markPreviewFailure}from'../shared.js';
try{const cards=[...document.querySelectorAll('.track-card')],records=[...document.querySelectorAll('.record')];let selected=0,manual=false;const motions=records.map((record,i)=>{const m=animate(record,{rotate:[0,360],scale:[.94,1.06,.94]},{duration:3,ease:'linear'});m.pause();return m});const setSelected=index=>{selected=index;cards.forEach((card,i)=>card.setAttribute('aria-pressed',String(i===index)))};cards.forEach((card,i)=>card.addEventListener('click',()=>{manual=true;setSelected(i)}));const render=time=>{const t=((time%3)+3)%3;if(!manual)setSelected(Math.min(2,Math.floor(t)));motions.forEach((motion,i)=>motion.time=i===selected?t:0);cards.forEach((card,i)=>card.style.setProperty('--p',i===selected?String(t%1):'0'))};window.__PREVIEW_RUNTIME_ASSERT__=async()=>{motions[0].time=0;await new Promise(r=>requestAnimationFrame(r));const a=getComputedStyle(records[0]).transform;motions[0].time=1.2;await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)));const b=getComputedStyle(records[0]).transform;setSelected(1);const semantic=cards[1].getAttribute('aria-pressed')==='true';setSelected(0);return motions.every(m=>m.duration>=2.9)&&semantic&&a!==b};installPreviewController({id:'track-card-play-state-handoff',library:'motion@12.42.2',renderer:'dom',render,ready:Promise.resolve()})}catch(error){markPreviewFailure(error)}
