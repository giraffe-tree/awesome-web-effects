import createREGL from'regl';import{installPreviewController,markPreviewFailure}from'../shared.js';import{clamp,loopTime}from'./batch-b-utils.js';
try{const canvas=document.querySelector('#ripple-canvas'),ring=document.querySelector('#ripple-ring'),dpr=Math.min(devicePixelRatio||1,2),regl=createREGL({canvas,pixelRatio:dpr,attributes:{antialias:true,alpha:false}});let pointer=null,draws=0;const draw=regl({vert:`precision mediump float;attribute vec2 p;varying vec2 uv;void main(){uv=p*.5+.5;gl_Position=vec4(p,0,1);}`,frag:`precision highp float;varying vec2 uv;uniform vec2 pointer;uniform float time,aspect;float stripes(vec2 p){return smoothstep(.15,.85,.5+.5*sin(p.x*18.+sin(p.y*12.)*2.));}void main(){vec2 d=uv-pointer;d.x*=aspect;float r=length(d);float wave=sin(r*65.-time*8.)*exp(-r*11.);vec2 displaced=uv+normalize(d+1e-4)*wave*.025;vec3 a=vec3(.03,.16,.22),b=vec3(.93,.36,.24),c=vec3(.2,.82,.68);float s=stripes(displaced+vec2(time*.02,0));vec3 color=mix(a,b,s);color=mix(color,c,smoothstep(.22,0.,abs(displaced.y-.46-sin(displaced.x*7.)*.08)));color+=vec3(.8,.9,1.)*max(0.,wave)*.22;gl_FragColor=vec4(color,1);}`,attributes:{p:[[-1,-1],[3,-1],[-1,3]]},uniforms:{pointer:regl.prop('pointer'),time:regl.prop('time'),aspect:regl.prop('aspect')},count:3,depth:{enable:false}});function resize(){const w=Math.round(innerWidth*dpr),h=Math.round(innerHeight*dpr);if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;canvas.style.width=`${innerWidth}px`;canvas.style.height=`${innerHeight}px`;regl.poll()}}function setPointer(x,y){const b=canvas.getBoundingClientRect();pointer={x:clamp((x-b.left)/b.width,.08,.92),y:1-clamp((y-b.top)/b.height,.1,.9)}}canvas.addEventListener('pointermove',e=>setPointer(e.clientX,e.clientY));canvas.addEventListener('keydown',e=>{const m={ArrowLeft:[-.06,0],ArrowRight:[.06,0],ArrowUp:[0,.06],ArrowDown:[0,-.06]}[e.key];if(!m)return;e.preventDefault();pointer=pointer??{x:.5,y:.5};pointer={x:clamp(pointer.x+m[0],.08,.92),y:clamp(pointer.y+m[1],.1,.9)}});window.__PREVIEW_RUNTIME_ASSERT__=()=>typeof draw==='function'&&!regl._gl.isContextLost()&&draws>0;installPreviewController({id:'pointer-following-displacement-ripple',library:'regl@2.1.1',renderer:'webgl',render:t=>{resize();const phase=loopTime(t)/3*Math.PI*2,p=pointer??{x:.5+Math.cos(phase)*.27,y:.5+Math.sin(phase)*.24};regl.clear({color:[.01,.02,.03,1]});draw({pointer:[p.x,p.y],time:loopTime(t),aspect:innerWidth/innerHeight});draws++;ring.style.left=`${p.x*100}%`;ring.style.top=`${(1-p.y)*100}%`},ready:Promise.resolve()})}catch(error){markPreviewFailure(error)}
