import{animate}from'motion';import{installPreviewController,markPreviewFailure}from'../shared.js';import{cosineLoop,clamp}from'./batch-b-utils.js';
try{const stage=document.querySelector('#sticky-stage'),cards=[...document.querySelectorAll('.stack-card')],offsets=[[0,0,0],[-2,70,0],[-4,136,0]];const controls=cards.map((card,i)=>{const c=animate(card,{y:[offsets[i][1],i*8],scale:[1,1-i*.035],rotate:[0,(i-1)*1.4]},{duration:1,ease:[.22,1,.36,1]});c.pause();return c});let offset=0;stage.addEventListener('wheel',e=>{e.preventDefault();offset=clamp(offset+e.deltaY*.0014,-.8,.8)},{passive:false});stage.addEventListener('keydown',e=>{if(!['ArrowUp','ArrowDown'].includes(e.key))return;e.preventDefault();offset=clamp(offset+(e.key==='ArrowDown'?.12:-.12),-.8,.8)});window.__PREVIEW_RUNTIME_ASSERT__=()=>controls.length===3&&controls.every(c=>c.duration===1&&typeof c.pause==='function');installPreviewController({id:'sticky-card-stack-accumulation',library:'motion@12.42.2',renderer:'dom',render:t=>{const p=clamp(cosineLoop(t)+offset);controls.forEach((c,i)=>c.time=clamp((p-i*.16)/(1-i*.16)))},ready:document.fonts.ready})}catch(error){markPreviewFailure(error)}
